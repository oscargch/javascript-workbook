string = 'whatever'

for (i=0; i < string.length; i++) {
  // console.log(string[0]);

  // if (i === i) {
  //   console.log("repeated character")

  // console.log(string[0]);

  if (string[0] === string[1] || === string[0] === string[3] === string[4] === string[5] === string[6] === string[7] === string[8] === string[9] === string[10]) {
    console.log("repeated letter")
  }
  }

  
  const string = 'hello';

  function detectDouble {
    for (i=0; i < string.length; i++) {
      for 
  }