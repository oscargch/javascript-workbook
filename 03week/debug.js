const unsortedScores = [37, 89, 41, 65, 91, 53];

sortedScores = unsortedScores.sort();
console.log(sortedScores.reverse());

// const sortedScores = unsortedScores.sort((a,b) => b-a)

function forEachFromScratch() {
// your code here

}

const newArr = [];

arr.forEachFromScratch
console.log(newArr);


//

forEachFromScratch(arr, function () {

})

//

function forEachFromScratch(arr, fn) {
  // your code here
  //use for loop to iterate over the array parameter
  //for every iteration call the function parameter
  //(fn(function))
  // call it witht he current value in the array, the current idex in the array, and the current array.
  }
  
  forEachFromScratch(arr, function () {
  
  })